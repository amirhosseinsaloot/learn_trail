"""Phase 8 — Search across approved learnings

See docs/SPEC.md, "Phase 8 — Search across approved learnings" section, for the full Build/AI-stack/Learn
breakdown. This file only encodes the exit criterion as an executable check.
"""

import pytest


@pytest.mark.phase
def test_phase_8_exit_criterion():
    """Phase 8 — Search across approved learnings.

    Exit criterion (verbatim, docs/SPEC.md):

        "Answers based on previous learnings identify exactly which approved learning supplied the context."

    TODO(phase 8): replace this placeholder with a real assertion for the
    criterion above once Phase 8 is implemented. Until then it must stay red.
    """
    pytest.fail("Phase 8 (Search across approved learnings) not yet implemented.")
