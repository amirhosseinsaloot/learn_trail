"""Phase 3 — Structured summary generation

See docs/SPEC.md, "Phase 3 — Structured summary generation" section, for the full Build/AI-stack/Learn
breakdown. This file only encodes the exit criterion as an executable check.
"""

import pytest


@pytest.mark.phase
def test_phase_3_exit_criterion():
    """Phase 3 — Structured summary generation.

    Exit criterion (verbatim, docs/SPEC.md):

        "Malformed or incomplete summaries are rejected before persistence."

    TODO(phase 3): replace this placeholder with a real assertion for the
    criterion above once Phase 3 is implemented. Until then it must stay red.
    """
    pytest.fail("Phase 3 (Structured summary generation) not yet implemented.")
