"""Phase 9 — Prompt optimization

See docs/SPEC.md, "Phase 9 — Prompt optimization" section, for the full Build/AI-stack/Learn
breakdown. This file only encodes the exit criterion as an executable check.
"""

import pytest


@pytest.mark.phase
def test_phase_9_exit_criterion():
    """Phase 9 — Prompt optimization.

    Exit criterion (verbatim, docs/SPEC.md):

        "The optimized program performs better on held-out examples, not only its optimization set."

    TODO(phase 9): replace this placeholder with a real assertion for the
    criterion above once Phase 9 is implemented. Until then it must stay red.
    """
    pytest.fail("Phase 9 (Prompt optimization) not yet implemented.")
