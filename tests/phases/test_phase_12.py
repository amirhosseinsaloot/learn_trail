"""Phase 12 — Advanced learning features

See docs/SPEC.md, "Phase 12 — Advanced learning features" section, for the full Build/AI-stack/Learn
breakdown. This file only encodes the exit criterion as an executable check.
"""

import pytest


@pytest.mark.phase
def test_phase_12_exit_criterion() -> None:
    """Phase 12 — Advanced learning features.

    PROXY criterion — docs/SPEC.md defines no verbatim exit criterion for this
    phase. This is a derived stand-in, not a quote from the spec:

        "At least one advanced learning feature (quiz generation, flashcards,
        spaced repetition, knowledge graph, learning-path recommendations,
        document ingestion, voice interface, MCP-based tools, or Markdown/PDF
        export) is implemented and covered by its own passing test."

    TODO(phase 12): replace this placeholder with a real assertion for the
    criterion above once Phase 12 is implemented. Until then it must stay red.
    """
    pytest.fail("Phase 12 (Advanced learning features) not yet implemented.")
