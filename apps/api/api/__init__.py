"""LearnTrail HTTP API.

Top layer of the docs/SPEC.md §16 layout: `api` may import `evals` and
`ai_core` and `database`; nothing under `packages/` may import `api`
(docs/CODE_QUALITY.md, import-linter contracts 1 and 2).
"""
